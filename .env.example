DATABASE_URL="postgres://postgres:password@localhost:5432/ticketing"
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_REGION="ap-southeast-1"
S3_BUCKET_NAME="your-ticketing-bucket"
FRONTEND_URL="http://localhost:3000"
NODE_ENV="development"
PORT="3000"
CORS_ORIGINS="http://localhost:3000"
SESSION_SECRET="replace-with-at-least-32-random-characters"
RESEND_API_KEY="re_replace-with-your-resend-api-key"
RESEND_FROM_EMAIL="support@quanbyit.com"
RESEND_FROM_NAME="Ticketing System"
# Server-to-server only. Use the same values in every integrated project backend.
TICKETING_PROVISIONING_TOKEN="replace-with-a-32-character-random-secret"
TICKETING_TICKET_API_KEY="replace-with-another-32-character-random-secret"
# Legacy RMIS-only names remain supported during migration; prefer the global names above.
RMIS_PROVISIONING_TOKEN=""
RMIS_TICKET_API_KEY=""
ADMIN_NAME="System Admin"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="replace-with-a-strong-password"
# Optional development seed bearer (shown here as a format only; generate your own secret).
SEED_API_KEY="qtk_live_replace-with-43-url-safe-random-characters"
